"use client";

import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#00ff80",
    },
    secondary: {
      main: "#ff0080",
    },

    info: {
      main: "#bdbdbd",
    },
  },
});

export default theme;
